# Session 2 — 用户提示词汇总

> 日期范围: 2026-06-26 ~ 2026-06-27
> 阶段: V1.5 规划 + 结构树管理 + 格式识别完善 + 大文件性能优化 + Bug 修复

---

## 2026-06-26

### 1. 启动 V1.5 规划
```
从 V1.3 继续，分步实现 V1.5 的功能
```

### 2. 配置 fewer-permission-prompts
```
/fewer-permission-prompts
```

### 3. 开始实现阶段 1
```
先实现阶段1
```

### 4. 结构树搜索回车无响应
```
结构树搜索框输入字段名称后回车，程序没反应，没有去匹配相应字段
```

### 5. 搜索后程序自动退出
```
回车搜索后程序并没有跳转到目标字段，过了几秒钟程序自动退出了
```

### 6. 搜索行为时灵时不灵
```
现在不会崩溃了，但搜索行为很奇怪，时灵时不灵的，好像和重名字段有关
```

### 7. 网络断开后继续
```
刚才网络断开了，请继续
```

### 8. 搜索乱跳
```
结构简单的可以，结构复杂（比如结构树有3层）的搜索行为还是很奇怪，会乱跳，时对时不对
```

### 9. 去掉启发式识别 + Unknown根节点
```
暂缓实现V1.5版其他规划功能，先做功能调整：去掉整个启发式识别功能，对打开时未能自动识别出格式的文件，在结构树窗口中显示一个名为"Unknown"的空的根节点；
```

### 10. 规划设计结构树管理功能
```
请规划设计结构树管理功能，要能在空节点基础上手工添加/删除/编辑子节点，子节点可嵌套，叶子节点要包含字段名称、文件偏移、数据长度（字节数）和数据类型；要支持导入和导出文件结构描述文件（json格式），文件模板可参考之前Rules目录下的导出文件进行设计
```

### 11. 设计魔数签名
```
魔数/签名部分要有的
```

### 12. 搜索导致程序退出
```
子节点范围校验弹出对话框点确定，程序自动退出
```

### 13. 子树嵌套深度限制
```
结构树子节点嵌套深度不得超过15层
```

### 14. 导入清除原树
```
导入结构时要先清除原来的结构树
```

---

## 2026-06-26 — 格式识别完善

### 15. 完善 MP4
```
接下来重点完善文件格式的识别功能，先完善MP4文件特征识别功能
```

### 16. 完善 JPG
```
请完善对JPG格式的支持
```

### 17. 最大文件大小 10GB
```
请将能打开的文件大小上限改为10GB
```

### 18. 完善 MKV
```
请完善对MKV格式的支持
```

### 19. 完善 EXIF JPG
```
请完善对EXIF扩展JPG格式的识别支持
```

### 20. 大文件性能优化
```
请专门优化打开大文件时的响应性能，不要影响程序界面操作
```

### 21. 2GB MKV 打开失败
```
打开一个2GB的MKV文件失败，程序状态栏提示：加载失败: HexRowList 不支持全量枚举，请使用索引器按需访问
```

### 22. 大文件响应仍慢
```
可以打开2GB的MKV文件了，但响应速度还是很慢，从打开到第一次显示十六进制视图超过3秒，后面点击结构树节点，程序几乎停止响应，要等近10秒才恢复响应，请再想想有没有什么可进一步提高响应速度的优化方法，可借鉴一下业内其他能极速打开和处理大文件的软件做法
```

### 23. RAR5 展开支持
```
请完善对RAR格式的支持
```

### 24. 右键菜单首次无响应
```
在结构树节点上点击右键没反应，没有弹出菜单
```

### 25. 右键响应慢
```
可以弹出菜单了，但响应过程有点慢，尤其是文件比较大的时候，添加了子节点后，结构树搜索功能和点击字段自动跳转功能都要等一段时间才能有效，早操作了没反应
```

### 26. 右键第一次无响应 + 范围校验
```
1、打开文件后第一次在结构树根节点上点击右键没反应，要点击第二次才弹出菜单；2、编辑子节点时应限制子节点覆盖的数据块范围不能超过父节点的覆盖范围
```

### 27. 魔数字节用法
```
魔数字节要怎么用？
```

### 28. 范围校验崩溃
```
子节点范围校验弹出对话框点确定，程序自动退出
```

---

## 2026-06-26 — 结构树管理

### 29. 压缩包展开节点区分 + RAR
```
1、请将展开压缩包新建的节点与结构树中的原节点区分显示；2、请将工具栏上的"识别"文字改为"重新识别"；3、增加完善对RAR压缩文件的支持
```

### 30. RAR5 展开失败
```
对RAR5格式支持有问题，展开压缩包时状态栏提示"未找到有效条目"
```

### 31. RAR5 官方说明
```
[发送 RAR5FileFormat.html 官方说明文档]
```

### 32. RAR5 仍然不行
```
试了，还是不行，没有展开出RAR文件中被压缩的文件，结构树保持不变
```

### 33. 发送测试文件
```
需要我把功能测试用的RAR文件直接发给你看看吗？
```

### 34. RAR4 也不行
```
还是不行，状态栏提示：未找到RAR4压缩条目。已把 test.RAR 文件放到项目的 samples/ 目录下
```

### 35. RAR5 不行 + 测试文件
```
还是不行，状态栏提示：未找到RAR4压缩条目。和之前一样，已把 test.RAR 文件放到项目的 samples/ 目录下
```

### 36. 发送 RAR5 文档
```
[发送 RAR5FileFormat.html]
我放到samples目录下了
```

### 37. RAR5 展开成功
```
可以展开了
```

---

## 2026-06-27 — Hex 视图修复

### 38. ASCII 截断 + 末行偏移
```
十六进制视图右侧的ASCII视图显示有点小问题：每一行的最后一个字节对应的ASCII被截断了没有显示，可能与字体有关，另外，最后一行会左移，侵占了左侧十六进制显示区域剩余的空白区域
```

### 39. 末行问题未修复
```
还是老样子，没有修复
```

### 40. 对齐已修复
```
现在是都对齐了
```

### 41. 高亮逻辑询问
```
请问目前点击左边结构树节点，右边十六进制视图的选中高亮逻辑是怎么样的？好像不完全正确
```

### 42. 高亮结束位置不对
```
高亮的起始位置一般是对的，但结束位置往往不对，感觉后面的点选会受前面点选的影响，每次选中高亮的区域有时一样，有时又都不完全一样
```

### 43. 高亮长度仍乱
```
基本正常了，但如果节点对应的数据长度很长的话，高亮显示功能还是会乱显示，有时会选中整个数据区，有时只选中偏移位置所在的1个字节
```

### 44. 高亮还是不一致
```
还是不太正常，但规律和之前不一样，切换节点时有时高亮的还是之前选中的节点数据，这种情况往往发生在特定节点之间的切换，切换到之外的其他节点又重新高亮了
```

### 45. 高亮全部正常
```
切换功能总算全部正常了
```

### 46. 分组和小端无效
```
十六进制视图上的分组和小端选择又无效了，若干版本之前是好的，怎么又没做好回归测试？
```

### 47. 程序无反应
```
程序运行不了，没反应
```

### 48. 分组和小端仍无效
```
程序可以正常启动了，但分组和小端仍然不起作用
```

### 49. 移除分组功能
```
还是无效，请完全去掉分组相关功能，只支持按字节显示就够了，先规划实施方案
```

### 50. 增加回归测试
```
增加回归测试，保证其他相关功能不受影响
```

### 51. 功能测试正常确认
```
功能测试正常
```

### 52. 消除运行时警告
```
请帮我消除这些运行时的警告：[dotnet build warning output]
```

### 53. 开发规范更新
```
开发规范已更新，请仔细阅读并更新相关文件
```

### 54. 汇总提示词
```
请汇总我之前的所有提示词，按时间顺序添加时间戳，转成易读的markdown格式保存到项目的sessions目录下，文件名就叫session2-prompts.md
```
