1、在写核心逻辑之前，先写好单元测试和集成测试用例。

2、在大型或复杂修改前，先进行规划（plan），通过与用户互动明确需求、消除歧义，形成方案文档，确认无误后再生成代码。

3、修改文件前先进行git commit，确保版本可回退。

4、对用户界面UI的简单设计可以用ASCII ART进行展示。

5、在代码中提供DEBUG开关，先按DEBUG模式开发交付，将程序运行中需要监控的关键信息以debug方式输出到log文件中，待用户测试通过后再切换回Release模式。

