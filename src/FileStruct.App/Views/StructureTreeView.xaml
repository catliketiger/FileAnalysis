<UserControl x:Class="FileStruct.App.Views.StructureTreeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:FileStruct.App.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 搜索框 -->
        <TextBox Grid.Row="0" x:Name="SearchBox"
                 Margin="4" Padding="4"
                 Text="" KeyDown="SearchBox_KeyDown"
                 ToolTipService.ShowDuration="10000"
                 ToolTipService.InitialShowDelay="100"
                 ToolTip="输入字段名称按 Enter 搜索定位"/>

        <!-- 结构树 -->
        <TreeView Grid.Row="1" x:Name="StructTree"
                  ItemsSource="{Binding StructureTree.RootItems}"
                  BorderThickness="1"
                  FontSize="12">
            <TreeView.Resources>
                <HierarchicalDataTemplate DataType="{x:Type local:TreeItemViewModel}"
                                          ItemsSource="{Binding Children}">
                    <StackPanel Orientation="Horizontal" ToolTip="{Binding Path=Node.Description}">
                        <TextBlock Text="{Binding Name}" FontWeight="Bold" Margin="0,0,4,0"/>
                        <TextBlock Text="{Binding Offset, StringFormat={}0x{0:X}}" Foreground="Gray" Margin="0,0,4,0"/>
                        <TextBlock Text="{Binding Length, StringFormat={}[{0}]}" Foreground="DarkGray"/>
                        <TextBlock Text="{Binding DataType}" Foreground="Blue" Margin="4,0,0,0"/>
                    </StackPanel>
                </HierarchicalDataTemplate>
            </TreeView.Resources>
        </TreeView>
    </Grid>
</UserControl>
