<UserControl x:Class="FileStruct.App.Views.BookmarkBarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="200" d:DesignWidth="280">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#F5F5F5" Padding="8,4">
            <TextBlock Text="书签" FontWeight="Bold"/>
        </Border>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="4">
            <Button Content="+" Width="24" Height="22" FontSize="14"/>
            <Button Content="−" Width="24" Height="22" FontSize="14" Margin="4,0,0,0"
                    Command="{Binding DeleteSelectedCommand}"/>
            <Button Content="✕" Width="24" Height="22" FontSize="12" Margin="4,0,0,0"
                    Command="{Binding ClearAllCommand}"/>
        </StackPanel>

        <ListBox Grid.Row="2" ItemsSource="{Binding Bookmarks}"
                 SelectedItem="{Binding SelectedBookmark}"
                 BorderThickness="0" FontSize="11"
                 MouseDoubleClick="BookmarkList_MouseDoubleClick">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Offset, StringFormat={}0x{0:X8}}" FontFamily="Consolas" Foreground="Gray"/>
                        <TextBlock Text="{Binding Name}" Margin="8,0,0,0"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
