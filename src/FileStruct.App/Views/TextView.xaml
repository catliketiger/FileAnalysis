<UserControl x:Class="FileStruct.App.Views.TextView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 编码工具栏 -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="4">
            <Label Content="编码:" VerticalAlignment="Center"/>
            <ComboBox x:Name="EncodingCombo" Width="120"
                      VerticalAlignment="Center" Margin="4,0"
                      SelectedIndex="0"
                      SelectionChanged="EncodingCombo_SelectionChanged">
                <ComboBoxItem Tag="utf-8">UTF-8</ComboBoxItem>
                <ComboBoxItem Tag="gb2312">GBK</ComboBoxItem>
                <ComboBoxItem Tag="utf-16LE">UTF-16LE</ComboBoxItem>
                <ComboBoxItem Tag="utf-16BE">UTF-16BE</ComboBoxItem>
                <ComboBoxItem Tag="ascii">ASCII</ComboBoxItem>
            </ComboBox>
        </StackPanel>

        <!-- 文本显示 -->
        <ScrollViewer Grid.Row="1" Background="{DynamicResource ContentBackgroundBrush}" Padding="8">
            <TextBox x:Name="TextContentBox"
                     IsReadOnly="True"
                     TextWrapping="Wrap"
                     FontFamily="Consolas"
                     FontSize="13"
                     BorderThickness="0"
                     Text="{Binding TextView.TextContent}"/>
        </ScrollViewer>
    </Grid>
</UserControl>
